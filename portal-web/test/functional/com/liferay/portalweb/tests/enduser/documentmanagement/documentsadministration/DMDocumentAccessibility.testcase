@component-name = "portal-content-management"
definition {

	property browser.chrome.version = "139.0";
	property portal.accessibility = "true";
	property portal.release = "true";
	property portal.upstream = "true";
	property testray.main.component.name = "Documents and Media";

	setUp {
		TestCase.setUpPortalInstance();

		task ("Given admin page") {
			User.firstLoginPG();
		}
	}

	tearDown {
		var testLiferayVirtualInstance = PropsUtil.get("test.liferay.virtual.instance");

		if (${testLiferayVirtualInstance} == "true") {
			PortalInstances.tearDownCP();
		}
	}

	@description = "LPS-186092 - Assert that the file upload page is accessible."
	@priority = 4
	test AssertFileUploadPageIsAccessible {
		task ("When accessing the multiple files upload page") {
			ProductMenu.gotoPortlet(
				category = "Content & Data",
				portlet = "Documents and Media");

			DMNavigator.gotoAddDocumentTypeCP(dmDocumentTypeName = "Multiple Files Upload");
		}

		task ("And when run axe-core, Then no error are returned") {
			AssertAccessible(value1 = "minor,moderate");
		}
	}

}