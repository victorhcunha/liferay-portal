Analytics.registerMiddleware(
	function(request) {
		request.context.experienceId = '${experienceId}';
		request.context.experienceName = '${experienceName}';
		request.context.experimentId = '${experimentId}';
		request.context.variantId = '${variantId}';

		return request;
	}
);