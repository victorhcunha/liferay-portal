suite: Backup Create Scheduler CronWorkflow
templates:
    -   templates/backup-create-scheduler.yaml
tests:
    -   asserts:
            -   hasDocuments:
                    count: 0
        it: Omits the scheduler CronWorkflow when backups are disabled
        set:
            backup.enabled: false
    -   asserts:
            -   hasDocuments:
                    count: 0
        it: Omits the scheduler CronWorkflow when no backup schedule is set
        set:
            backup.enabled: true
            backup.schedule: ""
    -   asserts:
            -   equal:
                    path: metadata.name
                    value: backup-create-scheduler-cron-workflow
            -   equal:
                    path: spec.schedules[0]
                    value: 0 5 * * *
            -   hasDocuments:
                    count: 1
            -   isKind:
                    of: CronWorkflow
        it: Renders the scheduler CronWorkflow with the configured schedule when backups are enabled
        set:
            backup.enabled: true