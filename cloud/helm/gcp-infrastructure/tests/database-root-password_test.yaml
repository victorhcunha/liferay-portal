release:
    name: my-release
    namespace: my-namespace
suite: Database Root Password
templates:
    -   templates/database-root-password.yaml
tests:
    -   asserts:
            -   equal:
                    path: metadata.name
                    value: my-release-db-root-password-generator
            -   isKind:
                    of: Password
        documentSelector:
            path: kind
            value: Password
        it: Names the password generator after the release
    -   asserts:
            -   hasDocuments:
                    count: 2
        it: Renders both the password generator and the ExternalSecret
    -   asserts:
            -   equal:
                    path: metadata.name
                    value: my-release-db-root-password
            -   equal:
                    path: spec.dataFrom[0].sourceRef.generatorRef.name
                    value: my-release-db-root-password-generator
            -   equal:
                    path: spec.target.name
                    value: my-release-db-root-password
            -   isKind:
                    of: ExternalSecret
        documentSelector:
            path: kind
            value: ExternalSecret
        it: Wires the ExternalSecret to the generated password